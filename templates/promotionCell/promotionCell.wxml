sssss

<template name="promotionCell">
  <view class="promotion">
    <image src="{{image}}"></image>
    <view class="promotion-cell-text">
      <view class="promotion-cell-title">{{title}}</view>
      <view class="promotion-cell-subTitle">{{subTitle}}</view>
    </view>
  </view>
</template>